Задача 1: створи таблицю де в колонках будуть дні тиждня, а в рядках час з 9-00 до 18-00, кожен рядок одна година. Вихідни клітинки пофарбуй у рожевий колір а будні у жовтий. Підказка цикл фор в циклі фор.

Задача 2: цикл for in в тебе є обʼєкт
const products = {
    milk: 2.5,
    bread: 1.0,
    eggs: 1.8,
    cheese: 3.2
};
створи функцію яка приймає такий обєкт та повертає обʼєкт типу const totalData = {products:[milk, eggs, ...], totalPrice: 8.5}

Задача 3: цикл for of створи функцію яка приймає масив чисел проходится по ньому і повертає середнє ціле значеня (округляти до ближчого) getAverage([1,2,3,4]) // 3

Задача 4: цикл for of створи функцію яка приймає масив чисел проходится по ньому і повертає масив видаляючи дублікати removeDuplicates([1,2,2,3,1,4]) // [1,2,3,4]

Задача 5*: цикл for of створи функцію яка приймає масив чисел проходится по ньому і повертає масив унікальних чисел getUniqElements([1,2,2,3,1,4]) // [3,4]

Задача 6** напиши простеньку гру. Це буде функція, яка отримує масив учасників забігу[{name: Bob, id: 123, skill: 0.6, distance, countOfSteps} // те на що буде множитись кожен конструкції}], та обʼєкт з конфігурацією забігу. {
  lapDistance: number, // дистанція одного кола умовно кажучі 100 (метрів)
  lapAmount: number, // кількість кол
  minStep: number, // мінімальна дистанція за один шаг
  maxStep: number, // максимальна дистанція за один шаг
  }
суть гри заключаєтся в тому щоб запустити фунцію яка запускає цикл while до того моменту всі учасники не пробіжуть повну дитсанцію кожен крок вираховуєтся наступним чмном беремо випадкове значеня від minStep до maxStep і множимо його на skill це знвченя буде результатом одного кроку , як тільки один з гравців пробіжить перше коло ми додамо в lapsSnapshots усіх учасників з їх поточним результатом. В конфігурацією отримати результат {result: [{name: Bob, id: 123, skill: 0.6, distance: 507, countOfSteps: 2467},....], lapsSnapshots: [[{name: Bob, id: 123, skill: 0.6, distance: 101, countOfSteps: 407},....]]}

Задача 7* цикл do while запитай юзера ввести пароль (можно через prompt), збережі це значеня в константу після чого запускай цикл в якому будеш питати ввести пароль. Якщо пароль співпав, то ми показуємо алерт що пароль правильний і більше не виконуємр цикл, якщо не співпав то знову показуємо промпт с запитом ввести пароль
