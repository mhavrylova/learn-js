Завдання 1: Калькулятор
Створіть калькулятор, який приймає два числа та операцію (наприклад, +, -, *, /) від користувача та виконує відповідну операцію за допомогою конструкції switch. Поверніть результат.

examples: calculate(2,2,+) // 4, calculate(2,3,*) // 6, calculate(2,2,?) // 'Un expected math operator'

Завдання 2: isWeekend
Напишіть функцію, яка приймає номер дня тижня (1-7) і повертає його назву за допомогою конструкції switch.

examples: isWeekend(1) // false isWeekend(5) // false, isWeekend(7) // true, isWeekend(34) // true, isWeekend(33) // false

Завдання 3: Перевірка на валідацію
Створити функцію на основі функціі валідаціі яку ми вже писали але за допомогою switch
підказка  switch(true) {blabla} // та пояснити як воно працює

Завдання 4: Розписати яка дія за якою виконуєтся. Може бути важко, не всі сіньори це вирішать, але якщо зрозумієшь, то потім буде ізі

const testObj = {a: 2}
1. const res1 = (true || false) && (5 + 3 * 2) ?? null;
2. const res2 = (3 * 2) + '5' ?? 0 || true;
3. const res3 = testObj?.b && 10 ?? testObj?.a && '5';
4. cosnt res4 = 0 || (2 ** 3) > 7 && (null ?? true);
5. cosnt res5withStar = (null && 99) ?? (true && 0 || !testObj?.name && (33 > '32') && 'undefined') ?? 'bla'
